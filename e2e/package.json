{
  "name": "anotherbeach-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "E2E test suite per Customer QR Tracker (M5+). Stack: Playwright TS, chromium only, target prod.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3"
  }
}
